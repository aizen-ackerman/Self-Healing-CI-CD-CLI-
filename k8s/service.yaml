apiVersion: v1
kind: Service
metadata:
  name: self-healing-service
  namespace: demo-app
spec:
  selector:
    app: self-healing-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
  type: NodePort
